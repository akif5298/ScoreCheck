# Fine-tuning dependencies for scorecheck-ocr
#
# Install PyTorch FIRST (not listed here — must match your CUDA version):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# Then install this file:
#   pip install -r scripts/requirements_finetune.txt
#
# Note: these are NOT part of the main project venv — install in a separate env
# or the system Python to avoid polluting the Node.js project's venv.

unsloth[colab-new]
datasets
trl>=0.15.0
pillow
transformers>=4.45.0
accelerate>=0.34.0
